/**
 * EvAudioSettings options for configuration
 */
export interface EvAudioSettingsOptions {
  /**
   * Show the "Initialize audio" banner when the browser blocks audio playback.
   */
  enableAudioInitPrompt?: boolean;
}
