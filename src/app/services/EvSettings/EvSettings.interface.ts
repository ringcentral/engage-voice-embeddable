/**
 * EvSettings options for configuration
 */
export interface EvSettingsOptions {
  // Optional configuration options
}

/**
 * Offhook state enum
 */
export type OffhookState =
  | 'connected'
  | 'disconnected'
  | 'connecting'
  | 'disconnecting';
